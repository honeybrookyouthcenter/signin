<UserControl
    x:Class="YouthCenterSignIn.Controls.GuardianControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:YouthCenterSignIn.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:DefaultBindMode="TwoWay"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel Spacing="15">
        <TextBox InputScope="NameOrPhoneNumber" Name="uiGuardianName" Header="Guardian's full name" Text="{x:Bind Guardian.Name, UpdateSourceTrigger=PropertyChanged}" />
        <TextBox Header="Guardian's phone number" InputScope="TelephoneNumber" Text="{x:Bind Guardian.PhoneNumber, UpdateSourceTrigger=PropertyChanged}" />
        <TextBox Header="Guardian's email (optional)" InputScope="EmailNameOrAddress" Text="{x:Bind Guardian.Email, UpdateSourceTrigger=PropertyChanged}" />
    </StackPanel>
</UserControl>
